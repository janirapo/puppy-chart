// copy this file into config/local.config.js in order to set local config
config = {
    mysql: {
        host: '',
        user: '',
        password: '',
        database: '',
    },
    environment: 'production',
    port: 3000,
    secret: 'setAuthSecretHere',
};

module.exports = config;
